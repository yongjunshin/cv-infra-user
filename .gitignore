# Case output — the directory itself is committed (verify/out/.gitkeep) because the
# platform mounts over it; what the sim writes into it is not.
verify/out/*
!verify/out/.gitkeep

# Python caches
__pycache__/
*.py[cod]

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
