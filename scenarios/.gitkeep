# Placeholder. Scenario YAML instances land here in P2~P3:
#   each = SUT image ref + scene + initial_pose + mission + criteria + adapter_config.
# Schema is owned by the platform (M1); this repo only instantiates it.
